version: "3.8"

services:
  dev1:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    shm_size: "16g"
    ipc: host
    volumes:
      - ../:/workspace:cached
    command: sleep infinity
    environment:
      - NVIDIA_VISIBLE_DEVICES=all